pipeline {
    agent any
 
    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'develop', credentialsId: 'github-credentials', url: 'https://github.com/Kundan-Arekar/jenkinslearning.git'
            }
        }
        stage('Build') {
            steps {
               echo 'Buil Started...'
            }
        }
        stage('Test') {
            steps {
                echo 'Test Started...'
            }
        }
        stage('Deploy') {
            steps {
                // Replace this with your deployment steps
                // This could involve copying files to a server, running a deployment script, etc.
                echo "Deploying the application..."
            }
        }
    }
 
}
